<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xiangnanzhang.myapplication" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ShowBookActivity" />
        <activity android:name=".ShowWebView" />
        <activity android:name=".MedalsActivity" />
        <activity android:name=".ImgBydListViewActivity" />
        <activity android:name=".OnMeasureActivity" />
        <activity android:name=".DialogActivity" />
        <activity
            android:name=".DialActivity"
            android:label="@string/title_activity_dial" >
        </activity>
        <activity
            android:name=".HttpRequestActivity"
            android:label="@string/title_activity_http_request" >
        </activity>
        <activity
            android:name=".Medals.RectActivity"
            android:label="@string/title_activity_rect"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
    </application>

</manifest>
